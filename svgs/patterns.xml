<?xml version="1.0" encoding="UTF-8"?>
<!--
    SVG files listed in here will have to adhere to some conventions:
        - the SVG tag must specify viewBox
        - the SVG width and height should use pixels as measurement unit
            - if other unit is used, may not appear as expected
        - the style tag should be a direct child of the svg tag
            - each style property must be on its own line.
            - colors must be in hex format
            - the selector must be on the same line as '{'
                - '}' must be on its own line
            - only one selector for a style group
                - supports tag names, class names, and id's only

    SVG files may need additional attributes/metadata set to communicate what 
    parameters can be attached to input
 -->
<patterns>
    <pattern>
        <file>morphing-diamonds.svg</file>
    </pattern>
    <pattern>
        <file>aztec.svg</file>
    </pattern>
    <pattern>
        <file>hlines.svg</file>
    </pattern>
	<pattern>
		<file>vlines.svg</file>
    </pattern>
    <pattern>
        <file>simplegrid.svg</file>
    </pattern>
    <pattern>
        <file>xbox.svg</file>
    </pattern>
</patterns>
